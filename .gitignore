# Data runtime — dibuat ulang sendiri oleh skrip.
# Di branch `data` berkas-berkas ini memang disimpan; di branch ini tidak.
state.json
data.json
run.log

# Sampah Python
__pycache__/
*.pyc

# Dependensi Node — ada sejak `middleware.js` memakai @vercel/functions.
# Vercel memasangnya sendiri saat build; yang di repo hanya package.json.
node_modules/

# Sampah editor / OS
.DS_Store
*.swp
.vercel
.env*
